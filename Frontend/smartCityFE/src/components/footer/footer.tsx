import styles from './footer.module.css'
export default function Footer(){
    return (
        <footer className={`flex-container flex-center ${styles.footer}`}>
            <h1>SMARTCITY</h1>
        </footer>
    )
}